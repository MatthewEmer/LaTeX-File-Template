\section{Introduction}

\subsection*{Abstract}

Thank you for choosing to use \LaTeX\ File Template. If you find any bugs or want some feature not currently included, then my contact information can be found on my GitHub: https://github.com/MatthewEmer.

\separate{7}This template is intended for academics (with special focus on those in the fields of computer science or mathematics) who want to write papers or other related documents in a consistent format which ties in with standard practises from other published papers. 

\separate{7}This guide consists of setup instructions to download the files and create your first document, before moving onto the custom commands/environments designed to speed up academic writing. Before using this guide, I would suggest that you have some practise with some basic \LaTeX\ commands using a guide such as https://www.overleaf.com/learn.

\separate{7}This documentation is accurate as of Version \templateversion.

\subsection*{Acknowledgements}

I would like to thank Dr Jamie Mason for his support and expertise in leading my discrete mathematics tutorials and seminars this past year, without which this project would probably not exist. His continuous feedback throughout has allowed me to hone the project from the messy original versions into a useable tool.

\separate{7}Thanks also to Joseph Eddon for supporting the development of this template through the creation of his own \LaTeX\ template, and for the feedback and suggestions he provided on multiple versions of the template throughout last year.

\newpage\section{Document Setup}

\subsection{Project Files}

The actual \LaTeX\ file template is contained in the folder \textit{The Template} and consists of five files: \textit{template.tex}, \textit{\_documentContent.tex}, \textit{\_appendix.tex}, \textit{\_templateformatting.tex}, and \textit{template.pdf}. To use the template, all four files must be downloaded and placed in the same folder. 

\subsubsection{\_templateformatting.tex}

This file contains the code required to create the template and the custom commands described in this document. It should not be edited.

\subsubsection{\_documentContent.tex}

This file allows you to add all the main content of the document you are trying to write. It will be inserted after the table of contents, but before the appendix (if you choose to add one). By default it contains an example section and definition. Only pages in this file are counted towards the page count of the document.

\subsubsection{\_appendix.tex}

This file allows you to add an optional appendix to the end of your document which is not counted towards your page count. By default it contains a unnumbered part title which has been inserted into the table of contents.  

\subsubsection{template.tex}

This file sets up the structure of the document, as well as acting as a compiler for the other \textit{.tex} files, ensuring they get inserted into the right sections of the document.

\separate{3}When creating your document, you need to edit lines 12-15 of this file to create the title page, alongside the document header and footer, as described in \elemref{tab:Lines to edit.}{Table}. 

\separate{3}If you want to change the generated pdf's name while still writing the document, this is the file name which needs to be changed.

\inserttable{c|c|c}{Lines to edit.}{\textbf{Line} & \textbf{Variable Name} & \textbf{Description}}
{
    12 & thetitle & The document's title. This will be placed in the header. \\
    13 & thesubtitle & The document's (optional) subtitle. This will be placed in the header. \\
    14 & theauthor & The list of authors. This will be placed in the footer. \\
    15 & thedate & Replace the code if you want a static date rather than date of last compile.
}

\subsubsection{template.pdf}

This file contains the output of the \LaTeX\ compiler. If you want to rename this document while still working on it, then delete this version, alongside any build files your compiler has created and change the name of \textit{template.tex}. 

\subsection{Optional Files}

\subsubsection{Storing Images}

If you choose to include figures in your project, then you will need to create a subfolder labelled \textit{Images} where you can place the image files for the project.

\subsubsection{GitHub Repositories}

If you are placing the document within a GitHub repository, you may want to copy over the \textit{.gitignore} from this repository as it is set up to ignore any \LaTeX\ build files generated by \LaTeX\ Workshop on Visual Studio Code. These are: \(\ast.aux\), \(\ast.fdb\_latexmk\), \(\ast.fls\), \(\ast.log\), \(\ast.out\), \(\ast.gx\), \(\ast.toc\), and \(\ast.gz\).

\subsection{Updating the Template}

If you have downloaded the template and a new version is released, then you can easily update your document to the new version by taking the \textit{\_templateformatting.tex} file currently in your project and replacing it the one from the new release. This is generally not advised for large releases (where the first digit of the version number changes) as other files may have been changed. 

\newpage\section{Custom Commands}

\subsection{General Tools}

\subsubsection{Element Spacing}

\numberedentry{cmd:separate}{Command}{\(\backslash separate\)

\separate{3}The separate command inserts vertical spacing between two elements whilst simultaneously removing the new paragraph indent from the following element. You can specify the amount of spacing as described in \elemref{tab:Parameters for separate.}{Table} and as seen in \preenvref{ex:cmd:separate}{Example}{entrynumber}.

\inserttable{c|c|c|c}{Parameters for separate.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Spacing & The number of points of vertical spacing. & 5
}

\noindent\example{cmd:separate}{\(\backslash separate\{3\}\) produces the separation as seen between the Command line and the description of separate.}}

\subsubsection{Text Formatting}

\numberedentry{cmd:colouredText}{Command}{\(\backslash colouredText\)

\separate{3}The colouredText command takes the given text and outputs it in a different colour, as chosen by the writer. This can be seen in \preenvref{ex:cmd:colouredText}{Example}{examplenumber} and as described in \elemref{tab:Parameters for colouredText.}{Table}.

\inserttable{c|c|c|c}{Parameters for colouredText.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Colour & The colour to change the text to. & blue \\
    2 & Text & The text you want outputted in colour. & Hello World
}

\noindent\example{cmd:colouredText}{\(\backslash colouredText\{blue\}\{\text{Hello World}\}\) produces \colouredText{blue}{Hello World}.}}

\separate{15}\numberedentry{cmd:comment}{Command}{\(\backslash comment\)

\separate{3}The comment command inserts notes into the document in red so they can be found more easily. This can be seen in \preenvref{ex:cmd:comment}{Example}{examplenumber} and as described in \elemref{tab:Parameters for comment.}{Table}.

\inserttable{c|c|c|c}{Parameters for comment.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Text & The text you want outputted in colour. & Hello World
}

\noindent\example{cmd:comment}{\(\backslash comment\{\text{Hello World}\}\) produces \comment{Hello World}.}}

\subsubsection{Date Formatting}

\numberedentry{cmd:yeardate}{Command}{\(\backslash yeardate\)

\separate{3}The yeardate command takes an adjacent date variable and reformats it into the four-digit year before outputting to the page as seen in \preenvref{ex:cmd:yeardate}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for yeardate.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{ & n/a & & }

\noindent\example{cmd:yeardate}{\(\backslash date\{\backslash yeardate \backslash today\}\) produces \date{\yeardate\today} (at time of writing).}}

\separate{15}\numberedentry{cmd:monthyeardate}{Command}{\(\backslash monthyeardate\)

\separate{3}The monthyeardate command takes an adjacent date variable and reformats it into the full month and four-digit year before outputting to the page as seen in \preenvref{ex:cmd:monthyeardate}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for monthyeardate.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{ & n/a & & }

\noindent\example{cmd:monthyeardate}{\(\backslash date\{\backslash monthyeardate \backslash today\}\) produces \date{\monthyeardate\today} (at time of writing).}}

\subsection{Tables and Figures}

\subsubsection{Inserting Tables}

\numberedentry{cmd:inserttable}{Command}{\(\backslash inserttable\)

\separate{3}The inserttable command simplifies the process of inserting tables into your document as described in \elemref{tab:Parameters for inserttable.}{Table} and seen in \preenvref{ex:cmd:inserttable}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for inserttable.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Columns & The required columns, separated by \(|\)s. & \(c|c|c|c\) \\
    2 & Caption & The caption to appear under the table. & Parameters for inserting tables. \\
    3 & Headers & The headers, separated by ampersands. & No. \& Name \& ... \\
    4 & Content & The content to be placed in the table. & 
}

\noindent\example{cmd:inserttable}{\(\backslash inserttable\{c|c|c|c\}\{\text{Parameters...}\}\{\text{No. \& Name \& ...}\}\{\text{1 \& Columns \& ...}\}\) produces \elemref{tab:Parameters for inserttable.}{Table}.}}

\subsubsection{Inserting Figures}

\numberedentry{cmd:insertimage}{Command}{\(\backslash insertimage\)

\separate{3}The insertimage command simplifies the process of inserting figures into your document as described in \elemref{tab:Parameters for insertimage.}{Table} and seen in \preenvref{ex:cmd:insertimage}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for insertimage.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & File name & The file name of the image (must be in \textit{Images}.) & durham.jpg \\
    2 & Figure caption & The caption to appear under the figure. & Durham Cathedral. \\
    3 & Width & The amount of the page width the image should occupy. & 0.3
}

\noindent\example{cmd:insertimage}{\(\backslash insertimage\{durham.jpg\}\{\)A large building sitting on top of a lush green hillside. (Stoll, 2022)\(\}\{0.3\}\) produces \elemref{fig:A large building sitting on top of a lush green hillside. (Stoll, 2022)}{Figure}.}

\insertimage{durham.jpg}{A large building sitting on top of a lush green hillside. (Stoll, 2022)}{0.3}}

\subsubsection{Referencing Tables and Figures}

\numberedentry{cmd:elemref}{Command}{\(\backslash elemref\)

\separate{3}The elemref command allows you to easily reference tables and figures with clickable links. This is described in \elemref{tab:Parameters for elemref.}{Table} and as seen in \preenvref{ex:cmd:elemref}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for elemref.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The label of the element being linked to. & tab:Parameters for elemref. \\
    2 & Type & The type of element being linked to. & Table
}

\noindent\example{cmd:elemref}{\(\backslash elemref\{\text{tab:Parameters for elemref.}\}\{\text{Table}\}\) produces the reference \elemref{tab:Parameters for elemref.}{Table}.}}

\subsection{Academic Environments}

\subsubsection{Basic Environments}

\numberedentry{cmd:definition}{Command}{\(\backslash definition\)

\separate{3}The definition command creates an environment for setting out a definition with automatic numbering with inbuilt labels so that you can reference the definition later in the document. Its parameters can be seen in \elemref{tab:Parameters for definition.}{Table} and an example of its use can be seen in \preenvref{ex:cmd:definition}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for definition.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The word being defined so it can be easily referenced. & example \\
    2 & Definition & The actual definition of the word (with the word underlined). & An \underline{example} is...
}

\noindent\example{cmd:definition}{\(\backslash definition \{\text{example}\}\{\text{An } \backslash underline\{\text{example}\} \text{ is what this is.}\}\) produces \preenvref{def:example}{Definition}{definitionnumber}.}

\separate{7}\definition{example}{An \underline{example} is what this is.}}

\newpage\noindent\numberedentry{cmd:conjecture}{Command}{\(\backslash conjecture\)

\separate{3}The conjecture command creates an environment for setting out a conjecture with automatic numbering with inbuilt labels so that you can reference the conjecture later in the document. Its parameters can be seen in \elemref{tab:Parameters for conjecture.}{Table} and an example of its use can be seen in \preenvref{ex:cmd:conjecture}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for conjecture.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The conjecture's name. & Someone's Conjecture \\
    2 & Conjecture & The content for the conjecture. & Someone's Conjecture states...
}

\noindent\example{cmd:conjecture}{\(\backslash conjecture \{\text{Someone's Conjecture}\}\{\text{Someone's Conjecture states something or other.}\}\) produces \preenvref{cnj:Someone's Conjecture}{Conjecture}{conjecturenumber}.}

\separate{7}\conjecture{Someone's Conjecture}{Someone's Conjecture states something or other.}}

\subsubsection{Environments with Proofs or Solutions}

\numberedentry{cmd:theorem}{Command}{\(\backslash theorem\)

\separate{3}The theorem command creates an environment for setting out a theorem with automatic numbering with inbuilt labels so that you can reference the theorem later in the document. Its parameters can be seen in \elemref{tab:Parameters for theorem.}{Table} and an example of its use can be seen in \preenvref{ex:cmd:theorem}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for theorem.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The theorem's name. & Example Theorem \\
    2 & Definition & The theorem definition. & If \(2x=y\) then \(8x=4y\). \\
    3 & Proof & The proof of the theorem. & If \(2x=y\)...
}

\noindent\example{cmd:theorem}{\(\backslash theorem \{\text{Example Theorem}\}\{\text{If } 2x=y \text{ then...}\}\{\text{If } 2x=y \text{, we...}\}\) produces \preenvref{thm:Example Theorem}{Theorem}{theoremnumber}.}

\separate{7}\theorem{Example Theorem}{If \(2x=y\) then \(8x=4y\) holds for all \((x,y) \in \N^2\).}
{
    If \(2x=y\), we can factor \(8x=4y\) by taking out a common multiplier of 4 to get \(4(2x=y)\). Therefore, the theorem must hold for all \((x,y) \in \N^2\).
}}

\separate{15}\numberedentry{cmd:lemma}{Command}{\(\backslash lemma\)

\separate{3}The lemma command creates an environment for setting out a lemma with automatic numbering with inbuilt labels so that you can reference the lemma later in the document. Its parameters can be seen in \elemref{tab:Parameters for lemma.}{Table} and an example of its use can be seen in \preenvref{ex:cmd:lemma}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for lemma.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The lemma's name. & Example Lemma \\
    2 & Definition & The lemma definition. & If \(2x=y\) then \(8x=4y\). \\
    3 & Proof & The proof of the lemma. & If \(2x=y\)...
}

\noindent\example{cmd:lemma}{\(\backslash lemma \{\text{Example Lemma}\}\{\text{If } 2x=y \text{ then...}\}\{\text{If } 2x=y \text{, we...}\}\) produces \preenvref{lem:Example Lemma}{Lemma}{lemmanumber}.}

\newpage\noindent\lemma{Example Lemma}{If \(2x=y\) then \(8x=4y\) holds for all \((x,y) \in \N^2\).}
{
    If \(2x=y\), we can factor \(8x=4y\) by taking out a common multiplier of 4 to get \(4(2x=y)\). Therefore, the lemma must hold for all \((x,y) \in \N^2\).
}}

\separate{15}\numberedentry{cmd:proposition}{Command}{\(\backslash proposition\)

\separate{3}The proposition command creates an environment for setting out a proposition with automatic numbering with inbuilt labels so that you can reference the proposition later in the document. Its parameters can be seen in \elemref{tab:Parameters for proposition.}{Table} and an example of its use can be seen in \preenvref{ex:cmd:proposition}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for proposition.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The proposition's name. & Example Proposition \\
    2 & Definition & The proposition definition. & If \(2x=y\) then \(8x=4y\). \\
    3 & Proof & The proof of the proposition. & If \(2x=y\)...
}

\noindent\example{cmd:proposition}{\(\backslash proposition \{\text{Example Proposition}\}\{\text{If } 2x=y \text{ then...}\}\{\text{If } 2x=y \text{, we...}\}\) creates \preenvref{prop:Example Proposition}{Proposition}{propositionnumber}.}

\separate{7}\proposition{Example Proposition}{If \(2x=y\) then \(8x=4y\) holds for all \((x,y) \in \N^2\).}
{
    If \(2x=y\), we can factor \(8x=4y\) by taking out a common multiplier of 4 to get \(4(2x=y)\). Therefore, the lemma must hold for all \((x,y) \in \N^2\).
}}

\separate{15}\numberedentry{cmd:standardproblem}{Command}{\(\backslash standardproblem\)

\separate{3}The standardproblem command creates an environment for setting out a standard problem with inbuilt labels so that you can reference the standard problem later in the document. Its parameters can be seen in \elemref{tab:Parameters for standardproblem.}{Table} and an example of its use can be seen in \preenvref{ex:cmd:standardproblem}{Example}{examplenumber}. When referencing a standard problem, the native \(\backslash hyperref\) command should be used.

\inserttable{c|c|c|c}{Parameters for standardproblem.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Number & The problem number. & 1 \\
    2 & Problem & The problem statement. & If \(2x=4\), what is \(x\)? \\
    3 & Solution & The problem's solution. & \(2x=4\), divide...
}

\noindent\example{cmd:standardproblem}{\(\backslash standardproblem \{1\}\{\text{If } 2x=4 \text{ what is } x\}\{2x=4...\}\) creates \hyperref[stpr:1]{Standard Problem \#1}.

\separate{7}\standardproblem{1}{If \(2x=4\), what is \(x\)?}{\(2x=4\), divide both sides by 2, \(x=2\).}}}

\subsubsection{Sub-Environments}

\textbf{Important Note}: Both of the following commands can only be referenced using \(\backslash elemref\) as opposed to the environment references used by the rest of this section. 

\newpage\noindent\numberedentry{cmd:proof}{Command}{\(\backslash proof\)

\separate{3}The proof command creates an environment for setting out a proof with inbuilt labels so that you can reference the proof later in the document. Its parameters can be seen in \elemref{tab:Parameters for proof.}{Table} and an example of its use can be seen in \preenvref{ex:cmd:proof}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for proof.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The proof's name. & Example Proof \\
    2 & Proof & The content for the proof. & Let \(x=...\)
}

\noindent\example{cmd:proof}{\(\backslash proof \{\text{prop:Example Proposition}\}\{\text{If } 2x=y...\}\) produces \elemref{prf:prop:Example Proposition}{Proof}.}}

\separate{15}\numberedentry{cmd:solution}{Command}{\(\backslash solution\)

\separate{3}The solution command creates an environment for setting out a solution with inbuilt labels so that you can reference the solution later in the document. Its parameters can be seen in \elemref{tab:Parameters for solution.}{Table} and an example of its use can be seen in \preenvref{ex:cmd:solution}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for solution.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The solution's name. & Example Solution \\
    2 & Solution & The content for the solution. & Let \(x=...\)
}

\noindent\example{cmd:solution}{\(\backslash solution \{1\}\{2x=4\text{, divide...}\}\) produces \elemref{sol:1}{Solution}.}}

\subsubsection{Customisable Environments}

\numberedentry{cmd:numberedentry}{Command}{\(\backslash numberedentry\)

\separate{3}The numberedentry command creates an environment for setting out a custom environment with automatic numbering with inbuilt labels so that you can reference it later in the document. Its parameters can be seen in \elemref{tab:Parameters for numberedentry.}{Table} and an example of its use can be seen in \preenvref{ex:cmd:numberedentry}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for numberedentry.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The entry's name. & cmd:numberedentry \\
    2 & Entry type & The type of entry. & Command \\
    3 & Content & The content of the entry. & The numberedentry command...
}

\noindent\example{cmd:numberedentry}{\(\backslash numberedentry\{\text{cmd:numberedentry}\}\{\text{Command}\}\{...\}\) produces \preenvref{entry:cmd:numberedentry}{Command}{entrynumber}.}}

\subsubsection{Referencing Environments}

\numberedentry{cmd:preenvref}{Command}{\(\backslash preenvref\)

\separate{3}The preenvref command allows you to easily reference academic environments with clickable links before they are defined in the document. This is described in \elemref{tab:Parameters for preenvref and postenvref.}{Table} and as seen in \preenvref{ex:cmd:preenvref}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for preenvref and postenvref.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Label & The label of the environment being linked to. & ex:cmd:postenvref \\
    2 & Type & The type of environment being linked to. & Example \\
    3 & Counter & The name of the counter for that environment. & examplenumber
}

\noindent\example{cmd:preenvref}{\(\backslash preenvref\{\text{ex:cmd:preenvref}\}\{\text{Example}\}\{\text{examplenumber}\}\) produces the reference \\\preenvref{ex:cmd:preenvref}{Example}{examplenumber}.}}

\separate{15}\numberedentry{cmd:postenvref}{Command}{\(\backslash postenvref\)

\separate{3}The postenvref command allows you to easily reference academic environments with clickable links after they are defined in the document. This is described in \elemref{tab:Parameters for preenvref and postenvref.}{Table} and as seen in \preenvref{ex:cmd:postenvref}{Example}{examplenumber}.

\separate{5}\example{cmd:postenvref}{\(\backslash postenvref\{\text{ex:cmd:preenvref}\}\{\text{Example}\}\{\text{examplenumber}\}\) produces the reference \\\postenvref{ex:cmd:preenvref}{Example}{examplenumber}.}}

\subsection{Mathematics}

\subsubsection{Basic Mathematics Notation}

\numberedentry{cmd:setnotation}{Command}{Insert standard number set symbols.}

\separate{3}This set of commands allows you to quickly insert the set symbols for all of the common number sets used in mathematics. The supported sets, and their associated commands, can be seen in \elemref{tab:Supported number sets.}{Table} below. 

\inserttable{c|c|c}{Supported number sets.}
{\textbf{Number Set} & \textbf{Command} & \textbf{Output}}
{
    Natural & \(\backslash N\) & \(\N\) \\
    Integers & \(\backslash Z\) & \(\Z\) \\
    Rational & \(\backslash Q\) & \(\Q\) \\
    Real & \(\backslash R\) & \(\R\) \\
    Imaginary & \(\backslash I\) & \(\I\) \\
    Complex & \(\backslash C\) & \(\C\)}

\subsubsection{Vectors and Matrices}

\numberedentry{cmd:dotproduct}{Command}{\(\backslash dotproduct\)}

\separate{3}The dotproduct command allows you to easily use the dot product vector operation within equations. The parameters for the function are described in \elemref{tab:Parameters for dotproduct.}{Table}, and an example can be seen \preenvref{ex:cmd:dotproduct}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for dotproduct.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Vector A & The vector on the left of the operation. & u \\
    2 & Vector B & The vector on the right of the operation & v}

\noindent\example{cmd:dotproduct}{\(\backslash dotproduct \{u\}\{v\}\) produces \(\dotproduct{u}{v}\).}

\separate{15}\numberedentry{cmd:jacobian}{Command}{\(\backslash jacobian\)}

\separate{3}The Jacobian command inserts the notation for the Jacobian matrix of a given function. This is described in \elemref{tab:Parameters for jacobian.}{Table}, and can be seen in \preenvref{ex:cmd:jacobian}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for jacobian.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Function & The function the Jacobian is generated from. & f}

\noindent\example{cmd:jacobian}{\(\backslash jacobian \{f\}\) produces \(\jacobian{f}\).}

\newpage\noindent\numberedentry{cmd:hessian}{Command}{\(\backslash hessian\)}

\separate{3}The Hessian command inserts the notation for the Hessian matrix of a given function. This is described in \elemref{tab:Parameters for hessian.}{Table}, and can be seen in \preenvref{ex:cmd:hessian}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for hessian.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{
    1 & Function & The function the Hessian is generated from. & f}

\noindent\example{cmd:hessian}{\(\backslash hessian \{f\}\) produces \(\hessian{f}\).}

\separate{15}\numberedentry{cmd:lagrangian}{Command}{\(\backslash lagrangian\)}

\separate{3}The Lagrangian command inserts the notation for a Lagrangian function. This is described in \elemref{tab:Parameters for lagrangian.}{Table}, and can be seen in \preenvref{ex:cmd:lagrangian}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for lagrangian.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{ & n/a & & }

\noindent\example{cmd:lagrangian}{\(\backslash lagrangian\) produces \(\lagrangian\).}

\subsection{Computer Science}

\subsubsection{Relational Algebra}

\numberedentry{cmd:joins}{Command}{Insert row join symbols.}

\separate{3}This set of commands allows you to quickly insert the symbols for all of the common join types used in relational algebra. The supported joins, and their associated commands, can be seen in \elemref{tab:Supported joins.}{Table} below. 

\inserttable{c|c|c}{Supported joins.}
{\textbf{Number Set} & \textbf{Command} & \textbf{Output}}
{
    Inner join & \(\backslash innerjoin\) & \(\innerjoin\) \\
    Left outer join & \(\backslash leftouterjoin\) & \(\leftouterjoin\) \\
    Right outer join & \(\backslash rightouterjoin\) & \(\rightouterjoin\) \\
    Full outer join & \(\backslash fullouterjoin\) & \(\fullouterjoin\)}

\subsubsection{Algorithms}

\noindent\numberedentry{cmd:insertalgorithm}{Command}{\(\backslash insertalgorithm\)}

\separate{3}The insertalgorithm command inserts a box suited for a pseudocode algorithm to be inserted into the document. This is described in \elemref{tab:Parameters for insertalgorithm.}{Table}, and can be seen in \preenvref{ex:cmd:insertalgorithm}{Example}{examplenumber}.

\inserttable{c|c|c|c}{Parameters for insertalgorithm.}
{\textbf{No.} & \textbf{Name} & \textbf{Description} & \textbf{Example Value}}
{ 
    1 & Algorithm name & The name of the algorithm. & Hello or goodbye \\
    2 & Inputs & The required inputs. & n/a \\
    3 & Outputs & Any returned values. & n/a \\
    4 & Pseudocode & The pseudocode of the algorithm. & \(\backslash Procedure\{\text{helloOrGoodbye}\}\{\}...\)
}

\newpage\noindent\example{cmd:insertalgorithm}{\(\backslash insertalgorithm \{\text{Hello or goodbye}\}\{\text{n/a}\}\{\text{n/a}\}\{\backslash Procedure\{\text{helloOrGoodbye}\}\{\}...\}\) \\produces \elemref{alg:Hello or goodbye}{Algorithm}}.

\insertalgorithm{Hello or goodbye}
{\(x \in \{1,2\}\)}
{A string message.}
{
    \Procedure{HelloOrGoodbye}{x}
        \If{(x == 1)}
            \Return \say{Hello World}
        \ElsIf{(x == 2)}
            \Return \say{Goodbye}
        \Else 
            \;\Return \say{418 I am a teapot.}
        \EndIf
    \EndProcedure
}